export default reducer = (state, action) => {
    console.log(this.props);
    switch (action.type) {
        case "show_all_list":
            return {

            };
        default:
            return state;
    }
}